# Henry PR review skill

Use `henry review <pr-number-or-url>` to inspect the full GitHub diff. Run six separate passes, produce actionable findings with exact file and line references, save the report, and place proposed comments in the approval queue. Use `henry approve approve <id>` followed by `henry approve send <id>` only after Luvish has reviewed the staged output.
