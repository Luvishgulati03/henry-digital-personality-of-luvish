# Memory

Use the actual Engram module. Keep Markdown as durable source material and SQLite as rebuildable index. Recall before turns, capture outcomes, reinforce useful associations, and dream on schedule.

## Luvish's repository preference

After a requested code change is reviewed and committed, push it to the repository's permitted personal remote without asking for a separate push confirmation. Luvish wants his GitHub contribution history and public body of work to grow continuously. This standing preference applies only to ordinary code pushes to an already configured, permitted personal remote. It does not authorize email, social posts, deployments with separate side effects, destructive Git operations, publishing private data, or bypassing repository safety checks.
